const createPR = async (req, res) => { try { const { repoUrl, filePath, fixedCode, bugDescription } = req.body; const repoOwner = await githubService.getRepoOwner(repoUrl); if (repoOwner !== githubService.getBotUsername()) { const hasPermission = await githubService.checkPermission(repoUrl); if (!hasPermission) { return res.status(403).json({ error: 'Bot does not have permission to create PR in this repository' }); } } const result = await githubService.createFixPR(repoUrl, filePath, fixedCode, bugDescription); res.json({ success: true, ...result }); } catch (error) { if (error.status === 404) { return res.status(404).json({ error: 'Repository not found' }); } else { console.error('PR creation error:', error); res.status(500).json({ error: 'Failed to create PR' }); } } }